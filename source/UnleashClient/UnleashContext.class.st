Class {
	#name : 'UnleashContext',
	#superclass : 'Object',
	#instVars : [
		'userId',
		'environment',
		'currentTime',
		'appName'
	],
	#category : 'UnleashClient'
}

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> _PRAGMA_IS_ [

	"%%PRAGMA DECLARE
	(name: IS_UnleashContext isPool: true isConstant: false)
	(pool: IS_UnleashContext declarations: (
		(name: IS_appName isConstant: false)
		(name: IS_environment isConstant: false)
		(name: IS_currentTime isConstant: false)
		(name: IS_userId isConstant: false)
		(name: IS_instanceInterfaceSpec isConstant: false)
	))"
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> abtPrimFlushInterfaceSpecCache [

	IS_UnleashContext associationsDo: [: poolDictionaryAssoc | poolDictionaryAssoc value: nil].
	super abtPrimFlushInterfaceSpecCache
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> IS_appName [
	"Private - ** Warning ** This method is generated by VisualAge and should not
	be modified or deleted. This method is responsible for returning a featureSpec
	that describes the implementation of a particular feature of the receiver"

	^IS_UnleashContext::IS_appName notNil
		ifTrue: [IS_UnleashContext::IS_appName]
		ifFalse: [
		IS_UnleashContext::IS_appName := (AbtAttributeSpec new
		 attributeType: String;
		 setSelector: #appName:;
		 getSelector: #appName;
		 changeSymbol: #appName)]
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> IS_currentTime [
	"Private - ** Warning ** This method is generated by VisualAge and should not
	be modified or deleted. This method is responsible for returning a featureSpec
	that describes the implementation of a particular feature of the receiver"

	^IS_UnleashContext::IS_currentTime notNil
		ifTrue: [IS_UnleashContext::IS_currentTime]
		ifFalse: [
		IS_UnleashContext::IS_currentTime := (AbtAttributeSpec new
		 attributeType: DateAndTime;
		 setSelector: #currentTime:;
		 getSelector: #currentTime;
		 changeSymbol: #currentTime)]
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> IS_environment [
	"Private - ** Warning ** This method is generated by VisualAge and should not
	be modified or deleted. This method is responsible for returning a featureSpec
	that describes the implementation of a particular feature of the receiver"

	^IS_UnleashContext::IS_environment notNil
		ifTrue: [IS_UnleashContext::IS_environment]
		ifFalse: [
		IS_UnleashContext::IS_environment := (AbtAttributeSpec new
		 attributeType: String;
		 setSelector: #environment:;
		 getSelector: #environment;
		 changeSymbol: #environment)]
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> IS_instanceInterfaceSpec [
	"Private - ** Warning ** This method is generated by VisualAge and should not
	be modified or deleted. This method is responsible for returning a featureSpec
	that describes the implementation of a particular feature of the receiver"

	^IS_UnleashContext::IS_instanceInterfaceSpec notNil
		ifTrue: [IS_UnleashContext::IS_instanceInterfaceSpec]
		ifFalse: [
		IS_UnleashContext::IS_instanceInterfaceSpec := AbtInterfaceSpec new
		featureNamed: #appName put: self IS_appName;
		featureNamed: #environment put: self IS_environment;
		featureNamed: #currentTime put: self IS_currentTime;
		featureNamed: #userId put: self IS_userId]
]

{ #category : 'IS_generated',
  #vaVisibility : 'private' }
UnleashContext class >> IS_userId [
	"Private - ** Warning ** This method is generated by VisualAge and should not
	be modified or deleted. This method is responsible for returning a featureSpec
	that describes the implementation of a particular feature of the receiver"

	^IS_UnleashContext::IS_userId notNil
		ifTrue: [IS_UnleashContext::IS_userId]
		ifFalse: [
		IS_UnleashContext::IS_userId := (AbtAttributeSpec new
		 attributeType: String;
		 setSelector: #userId:;
		 getSelector: #userId;
		 changeSymbol: #userId)]
]

{ #category : 'Accessors' }
UnleashContext >> appName [
	"Return the value of appName."

	^appName
]

{ #category : 'Accessors' }
UnleashContext >> appName: aString [
	"Save the value of appName."

	appName := aString.
	self signalEvent: #appName
		 with: aString.
]

{ #category : 'Accessors' }
UnleashContext >> currentTime [
	"Return the value of currentTime."

	^currentTime
]

{ #category : 'Accessors' }
UnleashContext >> currentTime: aDateAndTime [
	"Save the value of currentTime."

	currentTime := aDateAndTime.
	self signalEvent: #currentTime
		 with: aDateAndTime.
]

{ #category : 'Accessors' }
UnleashContext >> environment [
	"Return the value of environment."

	^environment
]

{ #category : 'Accessors' }
UnleashContext >> environment: aString [
	"Save the value of environment."

	environment := aString.
	self signalEvent: #environment
		 with: aString.
]

{ #category : 'Accessors' }
UnleashContext >> userId [
	"Return the value of userId."

	^userId
]

{ #category : 'Accessors' }
UnleashContext >> userId: aString [
	"Save the value of userId."

	userId := aString.
	self signalEvent: #userId
		 with: aString.
]
